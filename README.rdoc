== README

$ git clone https://github.com/ismetacar/myrailsblog.git

$ cd myrailsblog

$ bundle install

$ rake db:migrate

(gerekirse schema.rb dosyası silinebilir. daha sonra rake db:migrate komutu verilir. )

$ rails s
